---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: secret-manager-dev
  namespace: dev
spec:
  project: default
  source:
    repoURL: https://github.com/microscaler/gitops-cluster-dev.git
    targetRevision: main
    path: deployment-configuration/profiles/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

